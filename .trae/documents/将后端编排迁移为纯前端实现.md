## 我的理解
- 你要在 `frontend` 里只使用豆包（火山Ark）与百度AI统一检索，完全删除 DuckDuckGo、RSS、以及其它兜底路径。
- 依然保持前端纯实现，不接触后端Python。

## 拟改造内容（仅前端）
- 删除/停用文件：
  - 移除 `src/lib/search.js`（DuckDuckGo）；移除依赖调用处。
  - `src/lib/fetcher.js` 可选保留但默认不使用（浏览器CORS限制抓取正文不稳定）。
- 新增文件：
  - `src/lib/baidu.js`：实现百度 AI Search Chat Completions（参考后端 `agents/baidu_adapter.py:187-286`），返回 `{ items, summary }`，并在401时进行 `Bearer → Bearer+`重试；后处理过滤视频/图片域名。
- 改造 `src/lib/orchestrator.js`：
  - 流程只包含：
    1) `planning`：展示时间窗与模式（仅AI）。
    2) `search`（mode: 'ai'）：调用百度AI统一检索，yield 批次数与采样站点说明（如有）。
    3) `doubao_gate`：把 `items+summary` 交给豆包质量判定，yield ready/confidence/reasons/refine_keywords/recheck_query。
    4) 若 ready=true：生成详细报告（豆包），yield `final`（含 `detailed_report/items`）。
    5) 若 ready=false：根据建议关键词/重检查询，对百度AI执行一次重检（mode: 'ai_recheck'），再判定。
    6) 若仍不满足：直接强制生成详细报告（豆包），并在日志中提示“材料有限”。
  - 不再触发 DuckDuckGo 与规则版过滤/摘要；可选保留简单筛选（中文/去重）以提高质量。
- 改造 `src/lib/llm.js`：保持现状；在 orchestrator 里作为核心调用（两处：质量闸口判定、详细报告生成）。
- 改造 `src/App.jsx`：
  - 增加百度API Key输入（`BAIDU_AI_SEARCH_APIKEY`）；豆包API Key仍保留；
  - 取消 DuckDuckGo 文案；过程日志使用中性术语：统一检索/质量判定/二次检索/完成。
  - 如果 Key 缺失，提示“请填写Key”并阻断分析（因为只保留 AI 路径）。

## API 与安全
- 两个Key在浏览器本地存储（localStorage），不上传；用于开发演示，生产需由后端或边缘函数持有密钥。
- 百度API：`https://qianfan.baidubce.com/v2/ai_search/chat/completions`，`Authorization: Bearer <key>`（必要时用 `Bearer+`）；参数包含 `model`、`resource_type_filter.web.top_k`、`search_source: baidu_search_v2`、`search_filter.site`（可选）。
- 豆包Ark：`https://ark.cn-beijing.volces.com/api/v3/chat/completions`；用于输出 gate 的 JSON 和最终 Markdown 报告。

## 验证
- 同时填入两个Key：可完成统一检索→质量判定→二次检索（如需）→详细报告；导出Markdown正常。
- 任一Key缺失：立即在UI提示并阻断执行。

请确认以上方案，我将在 `frontend` 中完成对应文件的新增和修改，并彻底移除 DuckDuckGo/规则版路径。