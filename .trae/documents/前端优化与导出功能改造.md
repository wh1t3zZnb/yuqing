## 目标
- 优化页面“过程日志”的文案，去掉具体产品/平台字眼，改为更专业中性表达。
- 为“详细分析报告”新增前端导出按钮，点击后直接将报告以 Markdown 下载，无需调用后端。
- 所有改动仅在前端（templates/index.html）完成，不修改任何 Python 代码。
- API Key 暂存到 apikey.txt（本地开发用），线上部署时改为环境变量或安全托管，不在前端明文暴露。

## 文案优化（仅前端）
- 修改 `templates/index.html` 脚本中的日志文案：
  - 开始说明：将“由豆包（第二个AI）判断…”改为“由质量判定模块评估材料是否充足；不足则补充检索→仍不足进入经典检索路径”。
  - 搜索阶段：
    - `AI统一检索` 改为 `统一检索`；
    - `百度AI重检` 改为 `二次检索（补充）`；
    - 保留 `经典路径检索`，保持中性。
  - 判定阶段：
    - `豆包判定` 改为 `分析判定` 或 `质量判定`；
    - 细节字段如“置信度、判定理由、缺失维度”保持不变。
  - 其它：
    - `（兼容）打回：改为豆包判定材料不足…` 改为 `（兼容）打回：改为判定材料不足时给出补充建议并重检`。
- 变更点均在 `templates/index.html:136-334` 的 JS 事件处理 `appendStep` 输出处，涉及 `planning/search/doubao_gate/filter/fetch/judge/reject/final` 分支。

## 导出按钮（前端实现）
- 在结果面板中，当 `detailed_report` 到达并展示时，添加一个“导出 Markdown”按钮。
- 前端 JS 组装一个 Markdown 文本：
  - 标题含查询主题与时间；
  - 包含“快速摘要”和“详细分析报告”内容；
  - 可选附上“代表材料”列表（标题/来源/链接）。
- 使用 `Blob` + `URL.createObjectURL` 触发浏览器下载，例如文件名：`舆情报告-<查询>-<时间>.md`。
- 不调用后端 `/export`，满足“不要使用 python 脚本”的要求。

## API Key 管理（不在前端暴露）
- 本地开发：按你的要求，暂存到 `c:\Users\lbw\Desktop\trae\舆情分析\apikey.txt`（仅存储，不在前端读取）。
- 线上部署：
  - 建议通过环境变量（例如 `VOLC_API_KEY`、`ARK_API_KEY`、`LLM_API_KEY`、`BAIDU_AI_SEARCH_APIKEY`）或密钥管理服务注入；
  - 前端不会也不应直接使用或暴露密钥；涉及检索/LLM 的调用继续由后端处理，以避免泄露。
- 本次改动不会将任何 Key 写入前端代码或模板。

## 验证方案
- 启动本地服务后，输入查询：
  - 查看“过程日志”文案是否无平台词且更为专业；
  - 待生成“详细分析报告”后，按钮出现；点击后下载 `.md` 文件，打开内容结构完整。
- 后端流程不变；导出逻辑纯前端可在无后端 `/export` 时也正常工作。

## 开发日志（计划）
- 修改文件：`templates/index.html`
- 修改功能：
  - 优化日志文案的中性专业表达。
  - 添加前端“导出 Markdown”按钮与下载逻辑。
- 修改原因：满足你的四点要求，避免使用 Python 端实现导出，前端自洽。

请确认以上方案，我将按此直接在前端完成改动并交付。